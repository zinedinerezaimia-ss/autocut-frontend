[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Redirect API calls to backend (à configurer avec ton URL Railway)
# [[redirects]]
#   from = "/api/*"
#   to = "https://ton-backend.railway.app/:splat"
#   status = 200

# SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
